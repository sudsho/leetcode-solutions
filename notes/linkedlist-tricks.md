# linkedlist tricks

- bidirectional bfs: swap fronts when one outgrows the other.
- bidirectional bfs: swap fronts when one outgrows the other.
- bisect_left vs bisect_right matters when there are dups.
- bidirectional bfs: swap fronts when one outgrows the other.
- bisect_left vs bisect_right matters when there are dups.
- knuth optimization for matrix-chain-like O(n^3) -> O(n^2).
- monotonic stack: when removing only when strictly smaller vs less-equal matters.
