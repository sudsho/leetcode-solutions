# alt: revisited in 2023, slight cleanup
from typing import List, Optional

# original idea kept; this version uses match/dataclass-style refactor where it helps.

# Trying a slightly different style: same algorithm, leaner control flow.
# Original (kept for diff history) was in `solution.py`.

class Solution:  # type: ignore[no-redef]
    pass
