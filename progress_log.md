# progress log

Quick notes on what I am working on. Informal.

## 2019-03-26

starting some leetcode practice. plan to do easy problems first.

## 2019-03-27

small note.

## 2019-03-27

small note.

## 2019-03-27

small note.

## 2019-03-27

started keeping notes per problem.

## 2019-03-27

learning about binary search bounds.

## 2019-03-27

small note.

## 2019-03-27

small note.

## 2019-03-27

small note.

## 2019-03-27

small note.

## 2019-03-31

started keeping notes per problem.

## 2019-04-09

small note.

## 2019-04-20

more practice this week. trying to be consistent.

## 2019-04-20

small note.

## 2019-04-22

small note.

## 2019-04-26

small note.

## 2019-04-26

small note.

## 2019-04-29

small note.

## 2019-04-30

small note.

## 2019-04-30

small note.

## 2019-05-02

busy with school work, only a couple of problems this week.

## 2019-05-03

more practice this week. trying to be consistent.

## 2019-05-06

small note.

## 2019-05-09

learning about binary search bounds.

## 2019-05-09

small note.

## 2019-05-09

small note.

## 2019-05-13

small note.

## 2019-05-14

small note.

## 2019-05-25

on a streak. picking up tree problems next.

## 2019-05-28

small note.

## 2019-05-28

small note.

## 2019-06-01

short week. mostly cleanup and revisits.

## 2019-06-10

small note.

## 2019-06-17

small note.

## 2019-06-21

two-pointer pattern is showing up everywhere.

## 2019-06-24

small note.

## 2019-06-27

short week. mostly cleanup and revisits.

## 2019-07-04

short week. mostly cleanup and revisits.

## 2019-07-14

small note.

## 2019-07-19

small note.

## 2019-07-24

small note.

## 2019-07-26

small note.

## 2019-07-30

small note.

## 2019-08-05

more practice this week. trying to be consistent.

## 2019-08-11

small note.

## 2019-08-11

busy with school work, only a couple of problems this week.

## 2019-08-14

small note.

## 2019-08-15

small note.

## 2019-08-17

small note.

## 2019-08-21

small note.

## 2019-08-24

small note.

## 2019-08-24

busy with school work, only a couple of problems this week.

## 2019-08-27

small note.

## 2019-08-27

small note.

## 2019-08-31

small note.

## 2019-09-02

learning about binary search bounds.

## 2019-09-02

small note.

## 2019-09-20

small note.

## 2019-09-25

small note.

## 2019-09-27

started keeping notes per problem.

## 2019-10-02

small note.

## 2019-10-02

small note.

## 2019-10-02

looked into linked list problems. fast/slow pointer is neat.

## 2019-10-04

small note.

## 2019-10-09

small note.

## 2019-10-09

small note.

## 2019-10-09

small note.

## 2019-10-11

small note.

## 2019-10-14

small note.

## 2019-10-16

small note.

## 2019-10-17

small note.

## 2019-10-17

small note.

## 2019-10-19

small note.

## 2019-10-20

small note.

## 2019-10-21

small note.

## 2019-10-21

small note.

## 2019-10-24

started keeping notes per problem.

## 2019-10-28

small note.

## 2019-11-04

small note.

## 2019-11-05

small note.

## 2019-11-10

small note.

## 2019-11-11

started keeping notes per problem.

## 2019-11-13

more practice this week. trying to be consistent.

## 2019-11-14

two-pointer pattern is showing up everywhere.

## 2019-11-14

short week. mostly cleanup and revisits.

## 2019-11-21

small note.

## 2019-11-22

two-pointer pattern is showing up everywhere.

## 2019-12-02

small note.

## 2019-12-05

learning about binary search bounds.

## 2019-12-09

small note.

## 2019-12-09

small note.

## 2019-12-15

small note.

## 2019-12-17

small note.

## 2019-12-17

small note.

## 2019-12-19

small note.

## 2019-12-23

small note.

## 2019-12-23

small note.

## 2019-12-23

started keeping notes per problem.


## 2020-02-04

feeling more confident on trees.

## 2020-02-11

kadane variant for max product.

## 2020-02-22

topo sort with kahn's algo.

## 2020-02-22

got OrderedDict trick for LRU.

## 2020-02-23

graph problems are clicking finally.

## 2020-02-28

wrote a slow brute force first then improved.

## 2020-02-28

first time using heapq seriously.

## 2020-02-28

binary search variants are tricky.

## 2020-03-08

kadane variant for max product.

## 2020-03-19

monotonic stack is slick.

## 2020-03-27

deque is great for bfs.

## 2020-03-30

added a few alternate approaches.

## 2020-04-07

type hints starting to feel natural.

## 2020-04-17

exam week coming up. light practice.

## 2020-04-22

back at it after a quiet week.

## 2020-04-26

graph problems are clicking finally.

## 2020-04-27

small note.

## 2020-06-20

back at it after a quiet week.

## 2020-07-07

backtracking template helps a lot.

## 2020-07-15

back at it after a quiet week.

## 2020-07-22

added a few alternate approaches.

## 2020-07-24

walrus operator in Python 3.8 is fun.

## 2020-08-11

starting to see the medium difficulty problems.

## 2020-08-22

feeling more confident on trees.

## 2020-08-25

small note.

## 2020-09-09

two pointer pattern came up again.

## 2020-10-12

interval merging via sort first.

## 2020-10-19

more practice this week.

## 2020-10-19

backtracking template helps a lot.

## 2020-11-04

two pointer pattern came up again.

## 2020-11-09

kadane variant for max product.

## 2020-11-19

sliding window starting to feel natural.

## 2020-11-24

added a few alternate approaches.

## 2020-12-09

interval merging via sort first.

## 2021-01-21

lazy evaluation tricks.

## 2021-02-05

review week.

## 2021-03-27

type hints feel natural now.

## 2021-03-29

weekend long session.

## 2021-04-24

more graph problems this week.

## 2021-05-05

felt slow today, just one easy.

## 2021-05-05

small note.

## 2021-05-07

two heap trick for medians.

## 2021-05-19

got dataclass to play nice.

## 2021-06-17

review week.

## 2021-06-18

back at it after a quiet week.

## 2021-06-21

knapsack patterns are clicking.

## 2021-06-22

morning cleanup.

## 2021-06-22

small note.

## 2021-06-23

review week.

## 2021-06-28

string dp gives me trouble.

## 2021-07-10

type hints feel natural now.

## 2021-07-15

two heap trick for medians.

## 2021-07-30

comprehensions everywhere now.

## 2021-08-09

small note.

## 2021-09-14

interview prep mode.

## 2021-09-18

got dataclass to play nice.

## 2021-09-23

felt slow today, just one easy.

## 2021-09-28

comprehensions everywhere now.

## 2021-10-02

knapsack patterns are clicking.

## 2021-10-08

bfs with state for paths.

## 2021-11-13

dijkstra showed up twice this week.

## 2021-11-26

small note.

## 2021-11-29

bfs with state for paths.

## 2021-11-30

added a few alternates.

## 2021-12-05

morning cleanup.

## 2021-12-15

morning cleanup.

## 2021-12-18

trie problems are fun.

## 2021-12-20

weekend long session.

## 2022-01-11

thought about 71 again.

## 2022-01-17

cleaned up 71 a bit.

## 2022-01-17

problem 71: think i finally got the cleaner version.

## 2022-01-24

need to revisit 71 edge cases.

## 2022-01-26

started thinking about 71.

## 2022-01-30

back to 1819 after a short break.

## 2022-02-13

compared two approaches for 233.

## 2022-03-05

finished 71, will revisit.

## 2022-03-10

redid 71 with smaller helper.

## 2022-03-13

thought about 920 again.

## 2022-03-15

finished 920, will revisit.

## 2022-03-27

back to 1591 after a short break.

## 2022-04-01

reviewed 44, dp version makes sense.

## 2022-04-01

compared two approaches for 1819.

## 2022-04-05

back to problems.

## 2022-04-10

started thinking about 1707.

## 2022-04-13

compared two approaches for 920.

## 2022-05-05

back to problems.

## 2022-05-08

thought about 132 again.

## 2022-05-09

small note.

## 2022-05-10

tried a couple of mediums.

## 2022-05-13

problem 745: think i finally got the cleaner version.

## 2022-06-29

finished 803, will revisit.

## 2022-07-13

compared two approaches for 173.

## 2022-07-13

reviewed 165, dp version makes sense.

## 2022-07-14

back to problems.

## 2022-08-07

took notes on dp templates.

## 2022-08-17

tried a couple of mediums.

## 2022-08-27

small note.

## 2022-09-07

finished 1542, will revisit.

## 2022-09-13

back to problems.

## 2022-09-14

need to revisit 920 edge cases.

## 2022-09-24

took notes on dp templates.

## 2022-09-26

cleaned up 380 a bit.

## 2022-09-27

problem 1632: think i finally got the cleaner version.

## 2022-10-02

reviewed 127, dp version makes sense.

## 2022-10-24

compared two approaches for 30.

## 2022-11-10

small note.

## 2022-11-18

kept practicing today.

## 2022-11-24

problem 1106: think i finally got the cleaner version.

## 2022-11-30

started thinking about 123.

## 2023-01-13

morning cleanup.

## 2023-01-13

weekend long session.

## 2023-02-17

got dataclass to play nice.

## 2023-02-28

started thinking about a hard.

## 2023-03-10

knapsack patterns are clicking.

## 2023-04-02

got dataclass to play nice.

## 2023-04-08

comprehensions everywhere now.

## 2023-04-23

trie problems are fun.

## 2023-05-02

deep into hard problems lately.

## 2023-06-29

started thinking about a hard.

## 2023-07-08

kmp showed up twice this week.

## 2023-07-19

felt slow today, just one easy.

## 2023-08-26

back at it after a quiet week.

## 2023-09-02

trying match statements where they help.

## 2023-09-09

comprehensions everywhere now.

## 2023-09-21

knapsack patterns are clicking.

## 2023-09-30

felt slow today, just one easy.

## 2023-11-04

sweep line tricks again.

## 2023-11-27

interview prep mode.

## 2023-12-12

got dataclass to play nice.

## 2024-01-10

working on lc.
<!-- slot 10 2024-01-10T20:47:31+05:30 -->

## 2024-01-26

working on lc.
<!-- slot 28 2024-01-26T01:48:38+05:30 -->

## 2024-02-05

weekly contest debrief.

## 2024-02-20

working on lc.
<!-- slot 44 2024-02-20T19:20:52+05:30 -->

## 2024-02-29

reread classical hards.

## 2024-02-29

lc grind.

## 2024-03-30

working on lc.
<!-- slot 86 2024-03-30T11:54:33+05:30 -->

## 2024-04-03

tarjan notes.

## 2024-04-14

working on lc.
<!-- slot 93 2024-04-14T18:00:33+05:30 -->

## 2024-04-20

tarjan notes.

## 2024-05-17

monotonic stack template review.

## 2024-05-19

digit dp pattern.

## 2024-05-19

couples holding hands feels like cycle decomposition.

## 2024-06-28

thought about union find style.

## 2024-07-02

bitmask dp template note.

## 2024-07-03

small note.

## 2024-07-06

looking back at older mediums.

## 2024-07-06

looking back at older mediums.

## 2024-07-07

thought about union find style.

## 2024-07-25

reading editorial for a hard.

## 2024-07-26

tarjan notes.

## 2024-08-06

working on lc.
<!-- slot 181 2024-08-06T23:19:45+05:30 -->

## 2024-08-14

reading editorial for a hard.

## 2024-08-14

working on lc.
<!-- slot 186 2024-08-14T22:21:18+05:30 -->

## 2024-08-15

biweekly contest debrief.

## 2024-08-16

working on lc.
<!-- slot 189 2024-08-16T11:19:01+05:30 -->

## 2024-08-16

working on lc.
<!-- slot 190 2024-08-16T12:28:21+05:30 -->

## 2024-08-16

working on lc.
<!-- slot 192 2024-08-16T23:21:44+05:30 -->

## 2024-08-19

monotonic stack template review.

## 2024-08-20

working on lc.
<!-- slot 204 2024-08-20T22:59:57+05:30 -->

## 2024-08-24

tarjan notes.

## 2024-09-02

looking back at older mediums.

## 2024-09-18

weekly contest 350-ish, partial finish.

## 2024-09-18

leet practice today.

## 2024-10-03

digit dp pattern.

## 2024-10-27

monotonic stack template review.

## 2024-11-09

working on lc.
<!-- slot 249 2024-11-09T11:53:10+05:30 -->

## 2024-11-15

monotonic stack template review.

## 2024-11-15

bitmask dp template note.

## 2024-11-23

working on lc.
<!-- slot 260 2024-11-23T13:43:11+05:30 -->

## 2024-11-24

monotonic stack template review.

## 2024-12-10

weekly contest debrief.

## 2024-12-15

graph reroot dp note.

## 2025-01-13

monotonic stack patterns are clicking better now.

## 2025-01-13

closed off a couple of long-standing TODOs in notes/templates-2024.md.

## 2025-01-27

segment tree with lazy push works after a couple bugs.

## 2025-01-28

sliding window mins via monotonic deque. classic.

## 2025-01-31

revisited a tree dp; cleaner with reroot template.

## 2025-02-21

meet in the middle came in handy for closest sum.

## 2025-03-03

BIT plus offline sort is a workhorse.

## 2025-03-23

bitmask dp + small-to-large merge is a fun pairing.

## 2025-03-27

noting the contribution counting trick.

## 2025-04-04

sliding window mins via monotonic deque. classic.

## 2025-04-06

segment tree with lazy push works after a couple bugs.

## 2025-04-06

trying to write tighter binary-search predicates this month.

## 2025-04-10

BIT plus offline sort is a workhorse.

## 2025-04-10

cleaned up a sloppy alt I wrote last quarter.

## 2025-04-21

segment tree with lazy push works after a couple bugs.

## 2025-04-26

BIT plus offline sort is a workhorse.

## 2025-04-30

trying to write tighter binary-search predicates this month.

## 2025-06-16

closed off a couple of long-standing TODOs in notes/templates-2024.md.

## 2025-06-20

noting the contribution counting trick.

## 2025-06-23

bitmask dp + small-to-large merge is a fun pairing.

## 2025-07-03

BIT plus offline sort is a workhorse.

## 2025-07-08

revisited a tree dp; cleaner with reroot template.

## 2025-07-12

noting the contribution counting trick.

## 2025-07-19

mood board: combinatorics, modular inverse, and a strong cup of coffee.

## 2025-07-28

closed off a couple of long-standing TODOs in notes/templates-2024.md.

## 2025-08-06

closed off a couple of long-standing TODOs in notes/templates-2024.md.

## 2025-08-08

noting the contribution counting trick.

## 2025-09-01

BIT plus offline sort is a workhorse.

## 2025-09-08

BIT plus offline sort is a workhorse.

## 2025-09-26

some progress with z-array, still feels fragile.

## 2025-09-29

union find with size keeps appearing in the harder grid problems.

## 2025-10-02

some progress with z-array, still feels fragile.

## 2025-10-05

trying to write tighter binary-search predicates this month.

## 2025-11-21

practicing reading problem statements once and rephrasing.

## 2025-12-19

manacher feels less scary after reusing it twice.

## 2025-07-21

short entry today.


## 2026-01-11

## progress note

starting 2026 with vowel-window problems again.

## 2026-01-16

tidy up notes/ folder structure (no content yet).

## 2026-01-31

rough notes after a slow week.

## 2026-01-31

trying to space out hard problems more.

## 2026-02-01

reordered table around 33xx series.

## 2026-02-16

rough draft, will refine binomial mod 10 derivation.

## 2026-02-26

minor.

## 2026-02-27

stash before next week.

## 2026-03-18

snapshot before reading editorial.

## 2026-03-19

minor.

## 2026-03-28

resorted recent-activity bullets.

## 2026-03-29

removed stale TODO marker.

## 2026-04-03

remove duplicate trailing newline.

## 2026-04-05

minor.

## 2026-04-08

trim whitespace in two places.

## 2026-04-08

minor.

## 2026-04-08

minor.

## 2026-04-15

checkpoint.

## 2026-04-21

wrapping up march and april work.

## 2026-05-09

swap nodes in pairs - linked list, did iterative + recursive.

## 2026-05-10

length of last word - scan from right, easy one for sunday evening.

## 2026-05-13

spiral matrix - did the four-boundary walk. got tripped up on the one-row / one-column corner case until i added the top<=bottom and left<=right guards on the bottom and left passes. tests pass.

## 2026-05-14

bucket sort for maximum gap. the pigeonhole step took a minute to convince myself of - if there are n numbers and you set bucket width to ceil((max-min)/(n-1)), at least one bucket has to be empty, so the answer is across buckets. once that clicks the code is short. also knocked out valid palindrome and the at-most-twice variant of remove duplicates.

## 2026-05-16

late saturday, just one problem. zigzag conversion - the simulation approach with a direction flip on each boundary. cleaner than the math version where you index by row formula. wrapped early on numRows == 1 because dividing by (numRows-1) blows up otherwise.

## 2026-05-17

integer to roman. used the trick of putting the six subtractive pairs (CM, CD, XC, XL, IX, IV) directly into the value table so the greedy loop never has to undo anything. way cleaner than special-casing them after the fact. always forget which letters are subtractive vs additive until i write it out.

## 2026-05-19

divide two integers without using mul/div/mod. doubled the divisor with left shifts and subtracted each time it fit - basically binary long division. the only annoying part is the INT_MIN / -1 overflow case, returns INT_MAX. didn't bother negating INT_MIN in two's complement land since python ints are unbounded; just worked with abs values and tracked sign separately.

## 2026-05-20

combination sum ii. same shape as 39 but each candidate is single-use and the input can have dupes, so the combinations have to be deduped. sort the array, then in the loop skip when i > start and candidates[i] == candidates[i-1] - that drops duplicate values at the same recursion level but still lets the same value reappear deeper in the tree. also break when the current value exceeds the remaining target since we are sorted, saves a chunk of the recursion.

## 2026-05-28

number of 1 bits. went with the brian kernighan trick - n &= n - 1 clears the lowest set bit, so the loop only runs once per 1-bit. the obvious 32-iteration shift-and-mask works too but felt lazy. light day, just one problem.

## 2026-05-29

insert interval, the natural follow-on to 56. since the input is already sorted you don't need to re-sort - just walk it in three phases: copy everything that ends before new starts, absorb the overlapping run into one merged interval (mutating newInterval in place is fine here), then copy the tail. cleaner than building a fresh sorted list and rerunning merge. one and done for friday.

## 2026-06-02

burst day. cleared a chunk of medium gaps - multiply strings, spiral matrix ii, partition list, surrounded regions, plus pascals triangle as a warmup. multiply strings was the most satisfying, that schoolbook layout into an n+m buffer with the carry going into i+j and units into i+j+1 is one of those tricks you forget until you redo it. surrounded regions i finally stopped doing it inward-out - flooding from the border with a sentinel character is so much less error prone than tracking which O groups touch the edge. partition list was nothing, two dummy heads stitched at the end. ran long enough that i also tidied the readme table and did a small refactor on an older one.

## 2026-06-05

find all anagrams in a string. natural follow-on to the sliding window stuff from earlier in the week. the obvious way is to recompute the whole 26-letter window count and compare to p's count every step, but that comparison is O(26) per index. nicer trick: keep a running "matches" counter of how many of the 26 letters currently agree between window and target, and when the window slides only two letters change, so you fix up matches for just those two. window is an anagram exactly when matches == 26. kept a Counter-based variant in solution_alt for readability since the match-counting one reads a little dense.

## 2026-06-07

unique paths ii, the obstacle version of 62. lazy sunday so just one. the trick is realizing you don't need the full 2d table - one rolling row works because each cell only ever looks up (the value already sitting in dp[c] before you overwrite it) and left (dp[c-1]). obstacles just slam the cell to 0 so nothing routes through it. the one gotcha is the start cell: if the grid begins on an obstacle the whole thing is 0, so seed dp[0] conditionally instead of blindly setting it to 1.

## 2026-06-09

subsets ii. the dup-free version (78) is just append-current-then-recurse, but once the array has repeats you can produce the same subset down two different branches. sorting puts equal values adjacent, and then the one rule that fixes everything is: at a given recursion depth, only the first occurrence of a value may be chosen - any later sibling with the same value is skipped via the `i > start` check. tripped me up for a second because the guard is `i > start`, not `i > 0`; you DO want to pick the duplicate when it's the first element of a deeper branch (that's a genuinely new subset), you only skip it when it's a sibling at the same level. one and done for tuesday.

## 2026-06-18

did a little tree + linked list batch today. preorder iterative was the warmup - the only thing to remember is pushing right before left so left pops first. the one that took a beat was remove-duplicates-from-sorted-list-ii (82): it's deceptively different from 83. 83 keeps one copy of each value so you just skip-forward; 82 deletes the value entirely if it ever repeats, so you can't advance prev into a run - you have to walk cur to the end of the run and splice prev.next past the whole thing. dummy head pays off here because runs can start at the head.

## 2026-06-21

heavy sunday, felt like clearing a few. started easy with remove linked list elements (203) to warm up. classic case for a dummy head - the only thing that makes this annoying is when the nodes you want gone are right at the front, because then there's no prev to re-link. stick a sentinel before head and that whole branch disappears: prev sits on the last kept node, cur scans ahead, and you only advance prev when you actually keep something. return dummy.next at the end since the head itself may have been one of the deleted values.

then game of life (289), which i wanted to do without a second board. the rule you have to respect is that all cells flip simultaneously off the *current* grid, so if you write a plain 0/1 you poison the neighbour counts of cells you haven't visited yet. the bit-packing trick is clean: keep the original live/dead in bit 0 and stash the next state in bit 1, only ever reading `cell & 1` while you scan, then a second pass shifts everything right by one so the future becomes the present. constant extra space, two passes. the live/dead/born rules actually collapse nicely - survive on 2 or 3 if currently alive, born on exactly 3 if dead.

## 2026-06-22

majority element ii (229), the n/3 sequel to 169. the jump that makes it click is realizing there can be at most two values over n/3 - a third would push the total past n - so you run two boyer-moore counters side by side instead of one. the cancellation case is the bit i always have to slow down on: an element matching neither candidate doesn't just decrement one counter, it knocks a vote off *both*, same way the n/2 version pairs unequal elements. and unlike 169 the survivors aren't guaranteed, so you need the verify pass at the end counting actual occurrences before you trust them. monday warmup.

## 2026-06-28

quiet sunday, just wanted one. did construct binary tree from inorder and postorder (106), the mirror of the 105 i did a couple weeks back. same divide and conquer skeleton but two things flip. the root is now the *last* element of postorder instead of the first of preorder, and because i'm walking postorder backwards the roots come out as root, right, left - so when i recurse i have to build the right subtree before the left, otherwise the index pointer gets handed to the wrong side and the whole thing scrambles. inorder still does the same job: find the root's position and everything left of it is the left subtree, everything right is the right. hash the inorder indices once so each lookup is O(1) and it's linear overall.

wrote the iterative stack version too just to keep it honest. that one walks postorder from the back and uses inorder as a guide for when to stop going down the right spine and switch to attaching a left child - you pop while the top of the stack matches the current inorder value, and the last node you popped is where the left child hangs. fiddlier than the recursion but no call stack and it made me actually understand why the right-before-left ordering falls out.

## 2026-07-05

sunday but ended up doing more than i meant to. warmed up on excel sheet column number (171) which is just base-26 done left to right, except the sneaky part is there's no zero digit - A is 1 not 0, so AA is 27 and the whole thing is a *bijective* base rather than plain positional. once you see it's `result = result*26 + (ord(ch)-ord('A')+1)` it's the same loop as parsing a decimal string.

then maximum product of word lengths (318). the trick that makes it fast is that the alphabet is only 26 letters, so each word's letter *set* fits in a single int as a bitmask. two words share no letter exactly when `mask_i & mask_j == 0`, one AND instead of a set intersection. precompute all the masks once and the pairwise loop is cheap.

evening i took a swing at create maximum number (321), the hard one. it decomposes by how many digits come from each array: for a fixed split i/(k-i) the two picks are independent. picking the best length-L subsequence from one array is the monotonic-stack "remove digits to maximize" greedy. the merge is where i kept slipping - when the two front digits are equal you can't just grab either, you have to compare the whole remaining suffixes lexicographically to know which side to pull from, otherwise a bigger digit downstream gets stranded on the wrong side. comparing the python lists directly does exactly that look-ahead for free. try every split, keep the max.

## 2026-07-07

tuesday evening, did k closest points to origin (973). the whole thing hinges on not computing an actual distance - x*x + y*y orders points exactly the same as the real euclidean distance because sqrt is monotonic, so i skip the sqrt and dodge the float error too. kept a max-heap capped at k: push negated squared distances so python's min-heap hands me the *farthest* of the current k at the top, and the moment a closer point walks in i heapreplace it out. that's O(n log k) and only k things in memory, which is the version i'd actually want if the points were streaming.

wrote the quickselect alt as well since it's the textbook O(n)-average answer - partition around a pivot distance, and if the pivot lands exactly at index k you're done, otherwise recurse into whichever side still straddles k. in place, no heap, but the O(n^2) worst case on adversarial pivots is why i'd still reach for the heap by default. small thing i had to remember: the answer wants the k closest in any order, so once quickselect settles the boundary i just slice points[:k] without sorting.

## 2026-07-08

wednesday, felt like grinding so did a few. warmed up on the two intersection problems back to back (349, 350) since they're basically the same problem twice - 349 wants distinct values so it's just `set(a) & set(b)`, and 350 keeps multiplicity so you count one side and spend the counts as you walk the other. wrote the sorted two-pointer alt for both too because that's the answer the follow-ups fish for when the arrays are huge or already sorted.

then 132 pattern (456) which is the one i actually wanted to sit with. brute force is O(n^2) looking for i<j<k with nums[i] < nums[k] < nums[j]. the trick is to scan from the right keeping a monotonic-decreasing stack of candidate "3"s, and every time you pop something smaller than the current number you lift a `third` variable - that's the best possible "2" (a middle value that provably has a bigger element to its right). the instant any later number comes in below `third`, it's your "1" and you're done. took me a couple reads to convince myself the popped values are exactly the valid middles, but once it clicks it's a clean O(n).

## 2026-07-12

sunday afternoon, sat with max frequency stack (895). the design trap is thinking you need to reorder anything on pop. you don't. keep a freq map for how many copies of each value are live, and a second map from a frequency f to a stack of every value that has *reached* count f. when i push a value the second, third time it climbs into the group for 2, then 3, and so on, so each group ends up holding its members in push order for free. pop just grabs the top of the group at the current maxfreq - that value is simultaneously the most frequent and, among ties, the newest, which is exactly the two rules the problem wants. drop maxfreq by one when its group empties. O(1) both ways, no heap.

wrote the heap alt too since that's where my head went first: push (-freq, -seq, val) so a min-heap pops highest freq and newest-within-freq. correct but O(log n) per op and it carries a monotonic seq counter, so it's really just the grouped-stacks idea paying interest. kept it for contrast.

## 2026-07-13

monday, came back to reverse pairs (493) after doing count of smaller numbers after self (315) a few days back, since they're the same divide-and-conquer skeleton and i wanted to see where they diverge. both ride merge sort and count while the two halves are sorted, but the difference is the predicate. in 315 the "how many smaller sit to the right" question lines up with the merge comparison itself, so you can fold the counting straight into the merge loop. here the pair condition is nums[i] > 2*nums[j], and that factor of 2 doesn't agree with the plain merge order - a left value can beat 2*right yet still be less than right in the raw comparison - so trying to piggyback on the merge gave me wrong counts until i pulled the counting out into its own pass.

the clean version does a separate two-pointer sweep before merging: for each value in the sorted left half, push j forward across the right half while left > 2*right, and add j-mid. the key realization is j never resets - because the left half is sorted ascending, a bigger left value only ever needs j to move further right, so the whole sweep is O(n) per level and the total stays O(n log n). after counting i just let python sort the slice back for the merge, which keeps the code short.

wrote the fenwick alt too since 315 has one and i like having the counting-structure version side by side. compress the values and their doubled thresholds into one coordinate space, then sweep left to right asking how many already-seen values exceed 2*nums[i] before inserting nums[i]. the annoying part is the threshold and the value live in the same compressed axis, so i bisect on 2*v against the coord list rather than trusting the rank map for it. same O(n log n), just trades the recursion for a bit-indexed tree.

## 2026-07-15

wednesday, did odd even linked list (328) as a warmup after the heavier merge-sort stuff earlier this week. the problem reads harder than it is - "group odd indexed nodes before even indexed" made me reach for a dummy-node split at first, but you don't need the extra allocation. two runners is enough: odd starts at head, even at head.next, and each loop pass i splice odd to the node after even and even to the node after that, so both chains extend by one and the pointers leapfrog down the list. the only thing to be careful about is stashing even_head before the loop, because once i start rewiring odd.next i've lost the original second node otherwise, and that's exactly where the odd tail has to reconnect at the end. loop condition is even and even.next - even is always the trailing runner so if it or its next is gone there's nothing left to weave. clean O(n) one pass, no extra space.

## 2026-07-17

friday, knocked out 4sum ii (454). the naive read is four nested loops over the arrays, O(n^4), which is hopeless once n gets past a few hundred. the fix is meet in the middle: it's really a two-sum in disguise once you group the four arrays into two pairs. count every a+b from the first two arrays into a Counter, then for each c+d from the last two ask how many stored sums equal -(c+d). both passes are O(n^2) so the whole thing lands at O(n^2) time and space. worth noting this is 4sum ii, not 4sum - because we're returning a *count* of index tuples and the arrays are separate, duplicates aren't a headache the way they are in the classic 4sum, so no sorting or dedup dance here, just the two counters meeting in the middle.

## 2026-07-21

monday, did integer break (343). started with the DP because it's the obvious safe answer - dp[i] is the best product for i, and for each i you try every first cut j and pair it with either the whole remainder or its own best break, max(i-j, dp[i-j]). that inner max is the whole trick: it lets a "part" quietly be a further-broken chunk instead of a literal number. O(n^2) and done.

but the pattern in the dp table nagged at me - the splits kept landing on 3s. worked out why: for any part k >= 5 you're better off peeling a 3 off it because 3*(k-3) > k there, and 4 just ties with 2+2, so an optimal split never keeps a part bigger than 4 and prefers 3 over 2. so the closed form is just: cram in as many 3s as fit, then patch the remainder - remainder 2 leaves a plain 2, remainder 1 is the sneaky one because 3+1 wants to become 2+2, so you trade one 3 for a 4. n=2 and n=3 stay special since "at least two parts" forces a 1 into the split and caps them at 1 and 2. dropped it to O(log n) for the power. kept the dp as the alt since it's the one i'd actually write first in an interview before reaching for the math.

## 2026-07-23

trees day. wanted to nail down the BST mutation trio - insert, delete, and the greater-tree transform - since i always half-remember delete under pressure. started with insert into a bst (701) as the warmup because it's the gentle one: there's no rebalancing and any valid leaf position counts, so you just ride the invariant down (left when smaller, right otherwise) until you fall off the tree, and that empty child is exactly where the value preserves ordering. wrote it recursively first since the "root.left = insert(root.left, val)" reassignment pattern is the same skeleton i'll want for delete, then did the iterative walk as the alt - same path, just carrying a cursor and attaching to the first empty child instead of unwinding a stack. O(h) either way.

then the one i actually came for: delete node in a bst (450). the no-child and one-child cases are easy - return the surviving child and let the parent's "root.left = delete(...)" reassignment splice it out. the two-child case is the whole problem. you can't just yank the node, so you find its in-order successor (leftmost of the right subtree, the next-larger key), copy that value up into the node, then recursively delete the successor from the right subtree. the successor has no left child by definition, so that second delete always lands in an easy case and the recursion can't spiral. tempting to hand-splice the successor instead but routing it back through deleteNode keeps the pointer surgery in one place.

capped the day with vertical order traversal (987), which is really a bookkeeping-and-sorting problem wearing a tree costume. tag every node with (row, col) - root (0,0), left is (row+1, col-1), right (row+1, col+1) - bucket by column, done in one dfs. the trap is the tie-break: unlike plain vertical order, two nodes can share the exact same (row, col), and the spec wants those ordered by value. so it's not enough to keep a bfs order per column; you have to sort each column by (row, value) so the row drives top-to-bottom and value settles same-cell ties. once i sorted on the pair instead of trying to be clever with insertion order it fell out clean. nice bookend to the mutation problems - same trees, totally different muscle.

## 2026-07-25

prefix sum day. wanted to line up the whole family in one sitting because i keep re-deriving the same idea under a different disguise, so i went easy to hard on purpose. started with range sum query immutable (303), which is barely a problem once you notice the array never changes - build the prefix array in the constructor and every query is one subtraction. the only thing worth remembering is the padding zero at the front: with prefix[0] = 0 the formula prefix[right+1] - prefix[left] covers left = 0 for free, and without it you're writing a branch. good contrast with 308 which i did a couple weeks ago - the moment updates show up the prefix array is dead and you're into fenwick territory.

then contiguous array (525), which is the first one where the prefix sum isn't the answer itself but a key. equal counts of 0 and 1 doesn't look like a sum problem until you relabel 0 as -1, and then "equal counts" is literally "sums to zero", which is "two prefix sums are the same". after that it's a hash map of sum to index. spent a minute getting this wrong by overwriting the stored index each time i saw a sum - that quietly shrinks every window. you have to keep only the FIRST index, because the earliest occurrence is what makes each later match as long as possible. seeded with {0: -1} for the prefix that balances on its own.

capped it with number of submatrices that sum to target (1074), which is the payoff. four boundaries means O(m^2 n^2) submatrices if you go at it directly. the reduction is to fix a pair of column bounds, collapse each row to its sum across that band, and then the 2d question is just 560 on the collapsed column - count subarrays summing to target. so all three of today's problems are the same machinery with a different accumulator. the part that clicked: 525 stores first indices because it wants the longest window, 560 and 1074 store frequencies because they're counting. same map, different value, and picking the wrong one is how i'd have broken it. also had to remember to rebuild the counter per column band, since prefix sums from one band tell you nothing about the next.

## 2026-07-28

tuesday, went back for continuous subarray sum (523) because it was the obvious hole left after last week's prefix-sum run. it reads like a divisibility problem but it's the same hash map as 525 wearing different clothes: a slice is a multiple of k exactly when the two prefix sums bracketing it agree mod k, since the multiples cancel when you subtract. so instead of storing the running sum you store running % k and look for a repeat.

the length-at-least-two rule is where i actually lost time. a single element that happens to be a multiple of k produces the same remainder as the index right before it, so the naive "remainder seen before -> return true" passes on [5], k=5, which is wrong. guard with i - first_seen[r] >= 2 and it's fine. that guard is also the reason first-index-only matters here, same as in 525 - if you overwrite the stored index with the latest one you can shrink a legitimately wide window down under the floor and report false on a good array. so it's the third variant of the same map now: 525 keeps first indices to maximize a length, 560 and 1074 keep frequencies to count, and this one keeps first indices to prove existence under a width constraint. worth remembering that the modern constraints have k >= 1, so there's no divide-by-zero branch to write anymore.

## 2026-07-29

wednesday, started easy with find pivot index (724) as a warmup before getting back into the mod-k stuff. it's the most stripped-down member of the prefix-sum family and i almost skipped it, but writing it out made the padding-zero convention click in a way 303 didn't. the pivot condition is left == right, and the thing to notice is that right isn't independent information - once you have the total it's just total - left - nums[i]. so the prefix array isn't needed at all, only the running left sum, and the whole thing is O(1) space.

the one place to be careful is that the comparison has to happen before absorbing nums[i], since the pivot element belongs to neither side. i wrote it the other way round first out of habit and it silently shifted every answer by one. also worth remembering negatives are allowed here, so the sums aren't monotonic - nothing to binary search on, nothing to prune, and the plain left-to-right scan is also what hands you the leftmost index for free. kept the explicit prefix-array version as the alt just because it's literally the object 303 builds and it's nice to see the two next to each other.

then the one i actually opened the laptop for: subarray sums divisible by k (974). it's 523 with the question swapped from "does one exist" to "how many are there", and the reduction is character for character the same - a slice is divisible by k iff the two prefix sums bracketing it agree mod k. what changes is what the map holds. 523 keeps first indices because it needs the widest window to clear the length-2 floor; here there's no width constraint and every match is its own answer, so the map keeps a frequency and each step does total += counts[r] before counts[r] += 1. seed {0: 1} for the empty prefix, same reason 523 seeds {0: -1}.

that's the fourth time this month i've written this map and the value type is the entire difference between the variants, so i wrote it down: 525 first-index to maximize a length, 523 first-index to prove existence under a width floor, 560 / 1074 / 974 frequency to count. picking wrong is the failure mode, not the reduction.

negatives are the trap everyone warns about and python just handles it - -3 % 5 is 2, so a positive modulus already gives a non-negative remainder. worth noting for when i write this in java or c++ though, because there the same line splits one remainder class in two and quietly undercounts, and you need ((x % k) + k) % k.

also wrote the pairwise version as an alt: since any two prefixes sharing a remainder define a divisible slice, the answer is just sum of C(c,2) over the buckets. same number, and it makes it obvious the result depends only on bucket sizes and not on the order of the array, which is a nice sanity check on the incremental one.

capped it with make sum divisible by p (1590), which is where the family stops being a copy-paste. the move is to stop thinking about what's left and think about what gets removed: if target = total % p, the slice you delete has to carry exactly that excess, and you want the shortest one. target == 0 short-circuits to 0.

so it's still a remainder lookup, just displaced. prefix[i] - prefix[j] == target (mod p) rearranges to prefix[j] == prefix[i] - target (mod p), meaning at each right end there is exactly one earlier remainder worth asking about. 974 is the degenerate case where the shift is zero and you look up your own remainder.

the part i got wrong on the first pass was the map direction. i wrote last_seen but kept the first index out of muscle memory from 523/525 and got answers that were valid but too long. those two want the widest window so they keep the earliest index; this one wants the narrowest so it overwrites every time, because a later index with the same remainder is strictly better for every future right end. same dictionary, opposite update rule, and the thing that decides it is just whether the objective is a max or a min. that's the actual lesson from the last two weeks of these.

two guards: (running - target) % p needs the modulus or a negative intermediate misses the bucket, and best == n has to return -1 since deleting the whole array is banned even though the arithmetic is fine. three problems today all on the same map and i think i finally have the taxonomy straight.

## 2026-07-30

after three weeks of prefix sums it finally occurred to me that i'd only been using one direction of the trick, so corporate flight bookings (1109) today to do the other one. every problem since 303 has been values -> prefix sums so that a range *query* collapses to subtracting two entries. this one flips which operation is frequent: the queries are trivial (just read the array at the end) and it's the range *updates* that pile up, so you store the array pre-differenced and the prefix sum becomes the step that undoes it.

the naive version is a loop per booking and it's O(n*m), which is the whole reason the problem exists. in a difference array diff[i] = a[i] - a[i-1], adding a constant across a range only disturbs the two edges - the value steps up where the range opens and steps back down past where it closes - so a range add is two writes regardless of how wide the range is. accumulate once at the end and you're done.

the indexing is the only real hazard and i got it wrong first try, predictably. flights are 1-indexed, the array isn't, so the opening +seats goes at first - 1. the closing -seats goes at last, NOT last - 1, because it has to bite on the first flight outside the range and last itself is still inside it. i wrote last - 1 out of symmetry with the opening and lost the seats on every range's final flight. sizing the array n + 1 means a booking that runs to the last flight has somewhere real to put its cancellation instead of needing a guard, and that slot never gets read back.

worth writing down that the two are inverses of each other and which one you precompute is decided by which operation is hot: frequent queries + static data -> prefix sums, frequent range updates + one read at the end -> difference array. if both are frequent that's when you stop hand-rolling and reach for a fenwick tree, which is what 307/308 were already doing without me noticing the connection.

## 2026-07-31

friday, decided to spend the whole day on difference arrays since yesterday's 1109 was the first one and one problem is not enough to know a technique. warmed up with points that intersect with cars (2848), which is the same two writes per range but asks a different question of the accumulator - it wants coverage, not totals, so running is only ever tested as running > 0. that turns out to be the thing worth noticing: overlaps handle themselves. two cars over the same point push the count to 2 and it still reports covered exactly once, so there is no dedupe step anywhere.

also cleared up a small confusion i'd carried over from 1109. i wrote diff[start - 1] out of habit and it was wrong here - the -1 there was because flights were 1-indexed against a 0-indexed array, which is a fact about that problem and not part of the technique. the closing -1 at end + 1 IS part of the technique, because an inclusive range always has to cancel on the first point outside it. one of those two offsets travels and the other doesn't.

the reason this one is easy at all is end <= 100, so the array can just span the coordinate space. wrote the merge-intervals version as the alt because it's what actually survives the constraint being lifted - it costs O(n log n) in the number of cars instead of O(M) in the width of the line. the bit i fumbled there was adjacency: [1,3] and [4,5] leave no integer gap between them, so the merge condition is start > current_end + 1, not start > current_end. and max(current_end, end) when extending, since sorting by start tells you nothing about the ends and [1,10] followed by [2,3] would otherwise shrink the run.

afternoon: car pooling (1094), which is the same machinery for the third time and the first one where the accumulator has to be watched rather than reported. capacity is a constraint at every location, not at the end, so the comparison lives inside the sweep and returns False the moment it trips. checking after the loop is meaningless here since everyone has been dropped off by then and the total is always back to zero.

the off-by-one flips and it caught me, which is a good sign i'd been pattern-matching instead of thinking. passengers get off AT end, so the cancelling write goes at end, not end + 1. both previous problems had inclusive ranges where the close landed one past the last covered index, and i wrote end + 1 on reflex and got a car that was over capacity on paper for the length of one segment. so the rule is: the technique gives you "two writes, one accumulate", and the problem gives you where the second write goes. inclusive range -> r + 1, half-open -> r.

nice side effect of getting that right: a drop-off and a pickup at the same location both land in the same slot and net out before the comparison happens, which is exactly the "passengers exit before others board" rule the problem states. didn't have to write anything for it.

wrote the heap version as the alt since three problems in a row have leaned on a small fixed coordinate bound (100, 1000) and that's starting to feel like a crutch. sort by pickup, pop everyone whose drop-off is <= the current stop, then board. <= and not < for the same exit-before-board reason. it's O(n log n) in the trips instead of O(M) in the route length, and it's really the same algorithm - the heap is just holding the range updates that haven't closed yet, which is what the array's unaccumulated tail is doing implicitly.

evening, one more: shifting letters ii (2381). picked it because the range update isn't a number and i wanted to see where the technique stops applying. it doesn't stop - but the reason it works is worth naming. letter shifts compose additively and commutatively, forward 3 then back 1 is just forward 2 regardless of which order the shifts arrived in, so every position's pile of overlapping updates collapses to one net integer. that's the actual precondition for a difference array, and i'd been treating "there are range updates" as the trigger. it isn't. if the operation were order-dependent you'd have to replay the shifts in sequence and none of this would be available.

offsets went back to end + 1 here since the ranges are inclusive again. so that's three problems today with the closing write in two different places, which settles it - read the range semantics every time, the technique only promises you two writes and an accumulate.

the % 26 isn't just the z -> a wrap, the net shift can run to +/- len(shifts) so it's genuinely reducing. and python hands you the negative case for free, -3 % 26 is 23, same as the negative remainders in 974 last week. flagged it in the comments because it's the line that would silently break if i wrote this in java.

four of these in a day and the taxonomy is finally clean, so i wrote it into the notes file. the technique is fixed - two writes, one accumulate - and everything that varies is (a) where the closing write goes, decided by inclusive vs half-open, (b) what the accumulated value means, which was a total in 1109, a boolean in 2848, a live constraint in 1094, and a modular offset here, and (c) whether the coordinate space is small enough to index directly, which is what separates the array version from the heap/merge alts. three of today's four leaned on a tiny fixed bound and that's the part i'd be missing if the constraints were ever generous.

sunday, back to it after a day off. 2251, number of flowers in full bloom. picked it deliberately - friday's notes said the third thing that varies is whether the coordinate space is indexable, and i'd only ever hit "small fixed bound" or "merge the intervals instead". this is the case where the bound is 10^9 and there's genuinely no array.

the unlock was asking what the accumulate step was actually producing. it's the prefix sum of the diff array at position t, and every entry in that diff array is a +1 sitting at some start or a -1 sitting past some end. so the value at t is (starts <= t) minus (ends already finished), and both of those are counts in a sorted list. no array, two binary searches. the technique doesn't need the array at all, it needs the array to be cheap when you want every position - here i only want m of them.

the two bisects are different on purpose and that took a wrong submission to see. bisect_right on starts, bisect_left on ends. that asymmetry is the same inclusive-vs-half-open call as where the closing write goes in the array version, just moved somewhere else. same decision, third different syntax for it this week. still not something i can reuse from memory.

wrote the offline heap sweep as the alt again, which is now three problems running. not because it's faster - it isn't, sorting the queries costs more than binary searching them - but because it's the one that survives a harder question. if the problem asked which flowers instead of how many, the counting version is worthless and the heap already holds the answer. worth keeping that distinction: counts collapse, sets don't.

## 2026-08-03

monday. 2406, divide intervals into minimum number of groups. the statement is about assigning intervals to groups and i spent a while thinking about the assignment before noticing the question doesn't need one. two intervals have to be split exactly when they overlap, so the minimum group count is the size of the biggest all-mutually-overlapping set, and for intervals that set always shares a single common point - pairwise overlap is enough, you never get three intervals overlapping in pairs with no common point the way you can with arbitrary sets. so it's max coverage at a point, and that's the diff array for the fifth time.

what makes it worth doing is the accumulator. friday's taxonomy said the three things that vary are where the closing write goes, what the accumulated value means, and whether the coordinate space is indexable. this is a genuinely new entry in the middle one and i think it completes the list: the value's running MAXIMUM. 1109 read the final value, 2848 read it as a predicate, 1094 checked it against a bound at every step, 2381 reduced it mod 26, this one keeps the largest it ever got. the distinction that matters is consumed-vs-kept - 1094 tests the running total and throws it away each step, here it has to survive the whole sweep. so it's 1094's watching with 1109's reporting.

closing write at right + 1 since the endpoints are inclusive, array sized limit + 2 so the widest interval has a real slot to cancel into. that's now the fifth time writing that line and it did not take any thought, which is the first time this week that's been true.

the heap alt is the one i'd actually submit if the follow-up asked which group each interval goes in, because it's the only version that constructs an assignment instead of counting one. sort by start, keep one end time per open group, reuse the earliest-ending group if it's already finished or open a new one. only the earliest needs checking - if that one doesn't fit nothing does. the greedy is obvious once it's written down but i didn't see it before writing the counting version, which is backwards from how i'd like it to go.

third version is the two-pointer one and it's the one i learned something from. the sweep never needs to know which interval opened or closed, only how many, so the pairing between a start and its own end is dead information and you can sort the two endpoint columns completely independently. that felt wrong when i wrote it - throwing away which end belongs to which start seems like it must lose something - but the accumulator only ever adds one and subtracts one and doesn't care whose. it's 2251 from the other side: there the two sorted endpoint lists were binary searched at m query points, here they're walked once because every point is a query.

strict comparisons in both alts, ends[0] < left and ends[j] < start. intervals that touch at a point still overlap so <= would merge two groups that actually conflict. that's the inclusive/half-open decision showing up in its fourth different syntax in a week - as an index offset in the array version, as a bisect_left/bisect_right choice in 2251, and now as < vs <= in a loop condition. i still can't produce it from memory, i have to re-derive it from the range semantics every single time, and at this point i'm fairly sure that's correct rather than a gap - the technique genuinely doesn't fix it, the problem does.

## 2026-08-04

tuesday. went looking for the place last week's technique breaks, because five diff array problems in a row and the last one took no thought at all, which usually means i've stopped learning from the family rather than finished it. 699, falling squares.

the break is clean and it's worth writing down exactly. every one of those problems worked because the range updates composed - additively, commutatively, and without looking at the current state. that's what lets the pile collapse into one running number and the individual updates get thrown away. i noticed the commutativity half on friday with 2381, when i said out loud that order-dependent shifts would kill it. what i didn't notice is that there's a second condition sitting next to it, and this problem violates both.

a square writes (max height already under it) + side. so the update READS the state it's about to modify. and what it writes is an assignment, not an increment - it flattens what was underneath instead of adding to it, and there's no second write that undoes an assign the way -1 undoes +1. two squares in the other order give a different skyline. so there's nothing to accumulate and the structure has to hold the actual heights: query, then assign. segment tree with lazy propagation over the 2n compressed endpoints.

the lazy tag being an assignment rather than an addition is a smaller point but it's the one i got wrong first. an add-tag composes with whatever tag is already on the node. an assign-tag replaces it. i wrote the compose version out of habit and it passed the small tests, because you only see the difference when two assigns land on the same node before either gets pushed. also had to store the tag as None-or-value instead of testing truthiness, since height 0 is real and a falsy check drops it silently.

half-open interval here - squares that touch at a point sit side by side, they don't stack. that's the same decision as the closing write in the array version, the bisect_left/bisect_right split in 2251, and the strict < in 2406's alternates. fifth syntax for it in about a week, this time as the hi - 1 picking which compressed gap the assignment stops at. i still cannot produce it from memory and i've stopped expecting to. the technique doesn't fix it, the problem does.

wrote the O(n^2) brute force first and it's accepted on the real constraints, n is only 1000. kept it because it states the recurrence with nothing in the way. third version keeps an explicit skyline of disjoint flat runs and splices each drop in, and it's actually the fastest of the three at these sizes even though it's quadratic. the reason to keep it isn't speed though, it's that it holds the profile instead of a summary of it - if the follow-up wanted the skyline itself or the area under it, that version already has the answer and the tree would have to be walked to rebuild one. that's the third time this week the summary-vs-set distinction has decided which alt is worth keeping.

## 2026-08-05

wednesday. 2158, amount of new area painted each day. picked it because yesterday's break felt like it might be the only one, and i wanted to know whether "the diff array stops working" is one failure or several.

it's several, and this one is cleaner than yesterday's. 699 broke on composition - the update read what it wrote, the write was an assign, order mattered. none of that is true here. painting is idempotent, the final painted strip is just the union of the intervals, order is irrelevant to it, and a diff array will give me the coverage count of every cell without complaining. the count is correct and it is useless. the question asks which day got to a cell first, which is an argmin over the covering days, and summing them is exactly the operation that throws that away.

so the distinction i had been drawing all week is sharper than i was drawing it. i kept saying "counts collapse, sets don't" as a reason to keep the heap alt around for a hypothetical follow-up. here it isn't a follow-up, it's the actual question, and the accumulator isn't slow or wrong, it's answering something else. that's a better way to hold it: the diff array summarizes a pile of updates into a number, and every problem where that works is a problem where the number is the answer. 1109 wanted the total. 2848 wanted whether the total was nonzero. 1094 wanted whether it stayed under a bound. all numbers. this wants a name.

what saves it is the property 699 didn't have. the per-cell state is one bit and it only moves one direction - unpainted to painted, never back. so a cell changes at most once ever, total changes are bounded by the coordinate range instead of by the number of days, and the naive version's entire problem is that it re-walks cells that are already done. i wrote the brute force first for once instead of as an afterthought and it made the fix obvious rather than clever: the useful work is already linear, so stop doing the other work. nxt[i] = first unpainted cell at or after i, painted cells point right, path compression collapses a finished run to one hop.

wrote find iteratively. the recursive one is shorter and it dies, the chain can be the whole 5*10^4 range before compression and python gives up at 1000.

half-open again, this time as `cell < end` in the loop. sixth syntax in a week. i've stopped treating that as something i should have memorized by now - the technique genuinely doesn't fix which side is closed, the problem does, and the six versions aren't six facts, they're one decision wearing different clothes.

kept the sorted-disjoint-spans version as the alt for the usual reason and it's a sharper example than the last two. the union-find can tell me how much was new on day i but not where, because a compressed pointer has forgotten which run it jumped over - the information is destroyed by the optimization itself, not merely unrecorded. the span list still has it. that's the first time this week the alt survives a harder question for a structural reason rather than an incidental one.

## 2026-08-06

thursday. two days of hunting for where the diff array breaks, and both breaks were real, so today i wanted the opposite case on purpose - somewhere the technique doesn't break but has to stretch. 2536, increment submatrices by one. same range-update-then-report shape as the whole week, moved from a line to a grid.

the thing i actually wanted to know is whether 2d is a new technique or the old one applied twice, and it's the second. sweep every row into a prefix sum, then sweep every column, and a cell ends up holding the sum of every delta weakly north-west of it. that's it. the writes then have to be placed so the query rectangle nets +1, and since the accumulate is separable the writes are too: the 1d pair (+1 open, -1 past close) taken once per axis and multiplied out. which is the four corners. +1 at (r1,c1), -1 at (r1,c2+1) and (r2+1,c1) to kill the strips running right and down, +1 back at (r2+1,c2+1) for the quadrant that just got killed twice.

wrote the single-pass recurrence as an alt and it paid off more than i expected. grid[r][c] = diff[r][c] + grid[r-1][c] + grid[r][c-1] - grid[r-1][c-1], and the reason for the minus term is the identical double-count on the read side that the fourth corner write fixes on the write side. same four terms, same identity, twice. and that's the same shape as the 2d fenwick in 308 and the band collapse in 1074, both of which i'd filed under "prefix sum facts" as if they were separate things to remember. they aren't. it's one statement about rectangles and the diff array and the prefix sum are the two directions of it. that's the first genuine consolidation i've gotten out of this family instead of another entry in the taxonomy.

kept the separable version as the primary anyway. two independent 1d sweeps is a claim that the second axis adds nothing, and that claim is the whole point; the recurrence hides it behind something that looks like a 2d fact you'd have to memorize.

the row-wise alt is the useful one for a different reason. run a plain 1d diff array on each row the query spans - literally 1109 done r2-r1+1 times - and it's already correct. the only thing wrong is a full-height query costs n writes instead of 4. so the 2d version doesn't compute anything this one can't, it just moves the vertical spreading into a column sweep that every cell pays for regardless. the saving isn't a better idea about rectangles, it's noticing the work was already free. break-even is q*n vs n^2, so the row-wise one only loses once q reaches n, which here it does.

closing writes at r2+1 and c2+1, inclusive bounds. seventh syntax for that and the first time it's appeared twice inside one function. two independent copies of the same call in the same place is actually reassuring - it means the decision belongs to the range semantics, not to the dimension count, which is what i've been claiming all week without a clean example of it.

dropped the fourth corner write on the first attempt and the examples still passed. worth remembering why: it doesn't corrupt the rectangle, it corrupts everything south-east of it, and a small hand-checked grid usually has nothing there.

afternoon, second one: 1526, minimum number of increments on subarrays to form a target array. picked it because after this morning i wanted to know whether i actually understand the map or just the direction i keep running it in.

every problem in the family so far has gone forward - here's a pile of range updates, tell me what array they accumulate to. this one hands you the array and asks for the cheapest pile of updates that makes it. and the reason that's even answerable is a fact i've been leaning on all week without once saying out loud: the difference array is a bijection. array with an implied zero boundary determines its deltas, deltas determine it back. that's the actual license for two writes standing in for a whole range, and here it stops being background and becomes the whole problem.

so an op on [l,r] writes +1 to d[l] and -1 to d[r+1] and nothing else, d is pinned by the target, and d[i] = (ops starting at i) - (ops ending at i-1). second term is never negative so at least d[i] ops start at i. every op starts exactly once. total >= sum of the positive deltas, and that's the answer.

what struck me is that nothing gets simulated to get there. no sweep, no state, no accumulator running along the array. the bound is forced by what a single update is *allowed to write*, and it exists before any construction does. that's a different kind of argument than anything else this week and it's the first one where the technique gave me a lower bound rather than a computation.

the negative deltas being free took a minute to see and it's the same nesting fact as always - a close can always pair with something already open, so only opens are ever paid for.

wrote the divide and conquer version first, the flatten-to-the-min-then-split one. it's O(n^2) as i wrote it, O(n log n) with a sparse table, and the three line answer beats both. kept it for the usual reason and this is the fifth day running that same reason has picked the alt: it knows why each operation exists, and the linear version has collapsed that into a number. difference this time is that in 2251/2406/699/2158 the summary-vs-set call was justifying a slower *structure* against some hypothetical follow-up. here the fast version has no structure at all, it's three lines, so the alt is the only thing between the answer and a number i can't explain.

third version builds the ops so the bound is achievable rather than just asserted. stack of open starts, positive delta pushes, negative delta pops most-recent-first. LIFO isn't convenience - two ops over one delta sequence are nested or disjoint, never crossing, so the stack never has a choice to make. it's O(answer) not O(n), which is worth flagging since the target can be 10^5 everywhere.

target[0] as the leading term is d[0] = target[0] - 0. same implied zero boundary that's been putting the closing write one past the end since 1109. two problems today that both turned out to be about that boundary from different sides.

## 2026-08-07

friday. wanted the same backwards question as yesterday with one thing taken away, so 2772, apply operations to make all array elements equal to zero. every operation is a k-wide decrement, k fixed, and the question is just whether the array can be cleared.

taking the width away is the whole thing. in 1526 an op could be any width, which is why there were many multisets producing the target and why "which is smallest" was a sensible question. fix the width and index 0 can only be reached by an op starting at 0, so that count is pinned to nums[0]. that settles what index 1 still owes, and only an op starting at 1 can reach it, so that's pinned too. induction runs the length of the array and there is exactly one candidate multiset.

so there's nothing to minimize and that's why it returns a bool. i've been calling these sweeps greedy all week and here that word is actually wrong - greedy means choosing well at each step and there is never a second option to pass over. the algorithm doesn't pick the forced multiset, it just checks whether the forced multiset is legal.

two ways it isn't. remaining goes negative, meaning the ops already in flight overshot and nothing lifts it back since every op only subtracts. or the position still owes something and i + k runs past the end.

the second one is the interesting failure. yesterday i wrote down that negative deltas are free because a close can always be paired against something already open. that's a statement about having freedom in where the close goes, and fixing the width destroys it - the close lands at i+k and nowhere else. so the same fact that made 1526's bound achievable is the fact that makes this one able to fail outright. eighth syntax for the half-open boundary in about a week and the first time it's a source of infeasibility rather than an off-by-one to get right.

it's 995 with the state changed and the argument untouched. there the only way to fix a bit still reading 0 at i was to open a window at i, same forcing. flips compose mod 2 so that one carried a parity, decrements compose over the integers so this carries a count. nice to see which half of an argument is load-bearing.

alt returns the ops instead of the bool. wrote it because determinism is easier to see than to argue - what comes back isn't a solution, it's the solution, two callers can't disagree. and it flips the summary-vs-set call that's picked the alt six days running now. the previous five the summary was enough and the set was kept for some hypothetical follow-up. here the set is what the computation actually produces and the bool is the lossy thing on top of it.

kept the O(n*k) subtract-across-a-working-copy version too. too slow for the constraints, but the primary's counter is literally that inner subtraction deferred, and it makes the tail condition obvious - the loop stops at n-k so the last k-1 positions never get an op of their own and have to reach zero as a side effect. i + k > n is that same statement made one index at a time.

got the test harness wrong before the solution, which was mildly annoying. replayed the returned op counts to check they clear the array and indexed past the end on the trailing zero-count entries. the solution never writes there because a nonzero count already implies the window fits; the replay didn't know that.

## 2026-08-09

sunday, one problem, late afternoon. 2528, maximize the minimum powered city. picked it because friday ended on "fixing the width takes the choice away" and i wanted to know whether that's actually about the width.

it isn't. the width is fixed here too - every station covers exactly 2r+1 cities, same rigid window as 2772 - and the choice is completely back. what friday's problem was really leaning on was the target being an equality. clear the array *exactly*, so serving position i from anywhere left of i overshoots something already settled, and overshoot can't be undone because every op subtracts. that's what pinned the placement, not the width.

change the target to `power[i] >= x` and the whole thing loosens. a station serving city i can sit anywhere in [i-r, i+r]. everything left of i is already satisfied and more power can't hurt it. so there's no forcing left and the argument turns into an exchange - all legal placements cover i, the rightmost one also covers the most of what's still ahead, nothing behind objects. push right.

worth being honest that this is a weaker result than the one i wrote down friday. 2772 gave a unique multiset. this gives one optimal placement among many, and i can't check it the way i checked that one. friday's reconstruction was verifiable by having two callers agree, because there was nothing to disagree about. here two correct implementations can return different placements. so the test replays it instead - build what it says, recompute every city's power, check the minimum lands where claimed and the budget held. plus brute force over every distribution of k on small inputs, which is the only thing that actually tests the greedy rather than the arithmetic.

the other new thing is the layer. everything for a week and a half has been: sweep the array, the accumulator is the answer. this one is a maximin and the sweep can't produce it. binary search on x turns it into "is x feasible on budget k", that predicate is monotone, and the predicate is the same sweep as always. so the difference array stopped being the algorithm and became the oracle inside one. that's the first time it's shown up as a subroutine rather than the whole thing, and it's a better argument for why the technique is worth having than any of the standalone uses.

two failure modes from friday just evaporated and it's clean why. `remaining < 0` needed an exact target. the window falling off the end needed the placement pinned. neither survives, so the only way to fail is running out of budget, and the check is one running total against k. ninth syntax for the closing index, `min(i + r, n - 1) + r + 1`.

that clamp is the one detail i'd have called a boundary patch a week ago. it isn't - it's the statement that stations go in cities, and it self-justifies: if i + r >= n then i > n - 1 - r, so the last city is within r of i regardless. the window never needs to be truncated because there was never a placement outside the array to consider.

kept the linear scan alternate, targets one at a time instead of bisecting. way too slow, and the reason is the same reason as the last six days but pointing somewhere new. the primary fuses two independent ideas - the sweep is a feasibility test, the monotonicity is a separate claim about that test. bisection hides the second one. if the predicate weren't downward closed the binary search would return something confident and wrong, whereas the scan would just stop at the first no. wanted the assumption written where it could be seen.

left the unspent budget unspent, which looked like a bug until i wrote down why. anything built after the sweep is satisfied lifts some city above the minimum and can't lift the minimum. invisible to the objective. the greedy isn't trying to spend k, it's trying to certify x.

## 2026-08-10

monday, one problem, mid afternoon. 1552, magnetic force between two balls. wanted the same shape as yesterday one more time to see which parts of it were actually the technique and which parts i had just memorized.

the technique transferred whole. maximin, the sweep can't produce it, bisect on the answer, the pass becomes a feasibility oracle. wrote the outer loop without thinking about it, which is the first time that's happened with this shape.

the direction didn't transfer, and that's the useful bit. yesterday the greedy pushed right. today it pulls left. i spent a while assuming i had one of them backwards before noticing they're the same argument - leave the most freedom for whatever hasn't been decided yet - and the direction only flips because the thing extending forward changes sign. in 2528 it's coverage, which is a benefit, so more of it costs nothing. here it's the occupied prefix, which is a cost, and every unit of line spent behind is a unit the remaining balls can't have. so there was never a direction to memorize, only a sign to read off.

one place they genuinely differ though. yesterday's direction was forced - pull left in 2528 and you lose solutions, because a station left of i covers less of what's ahead and gains nothing back. today's isn't. reflect the coordinates and the rightmost-anchored greedy is exactly as correct, since distance is symmetric and nothing in the objective picks an end. put that in as maxDistanceMirrored rather than a comment, because it's the sort of claim i'd like to hear about immediately if i've misread it.

the feasibility pass is two exchange claims and i ran them together on the first pass. the anchor - some optimal arrangement uses position[0] - and the step - take the earliest legal spot. they're separate. the anchor is a slide argument on one gap, the step is about the length of the suffix left over. writing them apart made the second one obvious and the first one slightly less so than i'd assumed.

sorting matters here in a way it didn't yesterday. 2528 handed me the geometry in index order, city i at index i, so "further right" was a fact about the array. this is an unordered pile of coordinates and "as early as possible" is meaningless until the order exists. also only adjacent gaps get checked, which is the whole reason the pass is linear - once sorted, anything non-adjacent is separated by at least the sum of what's between it.

the pigeonhole upper bound removed a guard instead of adding one, which was a small pleasure after a week of patching boundaries after the fact. m balls in a span of W leave m-1 gaps summing to at most W. and since the positions are distinct integers the span is at least n-1 >= m-1, so the bound never drops below low=1 and the range can't come out empty. no special case for it.

placement alternate again, eighth day, same verdict as yesterday and now with a name for why. the witness isn't canonical because the mirror produces a different one, so callers can't be compared on placements. but they must agree on the gap, and checking two directions against each other is a better test than replaying one greedy against itself. kept the m == n shortcut separate too - it's the only input where the greedy makes no decision at all, which makes it the cleanest thing to check the general path against.

## 2026-08-11

tuesday, one problem, late afternoon. 2064, minimized maximum of products distributed to any store. picked it wanting the same outer loop a third time, mostly to find out whether the last two days had taught me a technique or just two problems.

they'd taught me two problems. the outer loop went in without thinking again, and then i sat waiting for the greedy and there isn't one. the predicate is sum(ceil(q_i/x)) <= n. arithmetic.

which retroactively reframes both of the last two days. i had been treating the exchange argument as though it came bundled with binary-search-on-the-answer - like the shape arrives with a greedy attached and the work is figuring out which way it points. it doesn't. the shape is two things, a monotone predicate and a bisect that finds its boundary, and everything past that is just whatever answering the predicate happens to take. it took a greedy twice in a row, i spent two days on the hardest part of those two problems, and i filed the hard part under the wrong heading. that's the real thing from today and it's a correction rather than an addition.

worth naming what buys the collapse, because it reads like a restriction rather than a gift. a store carries at most one product type. so products never compete for a store, the cost is additive over them, and the predicate decomposes. if stores could mix, feasibility would be a bin-packing question and none of this holds up. i read past that line the first time as a constraint to work around.

the lemma underneath is one inversion. q units over k stores leaves the fullest holding at least ceil(q/k) by pigeonhole, and an even split hits it exactly, so the best possible max for one product on k stores *is* ceil(q/k) - not bounded by, equal to. then ceil(q/k) <= x iff q <= kx iff k >= ceil(q/x). that's the whole solution. no search inside the search, which is what the last two days both had.

and monotonicity came free for the first time. 2528 and 1552 both needed the exchange argument settled before i could even claim the predicate was monotone. here it's ceil(q/x) shrinking as x grows. i notice that's the part i've been least careful about - bisection returns something confident whether or not the predicate is actually monotone, and twice now the proof of monotonicity was doing more work than the bisection was.

the bisection form flipped and i wrote it wrong first. yesterday was (low+high+1)//2 with low=mid, today is (low+high)//2 with high=mid. it isn't about maximizing versus minimizing, it's about which side keeps mid - the half that keeps it has to be the one that shrinks, so the rounding goes toward the other end. two days of thinking of that as a per-problem detail to remember.

upper bound is max(quantities) and it's always feasible, since the sum degenerates to m ones and m <= n is promised. so no empty-range guard. second day in a row a bound has removed a case instead of adding one, and it's the same cause both times: derived from the structure rather than picked big enough to be safe. also the unused stores, which looked wrong until i remembered writing the same paragraph about 2528's unspent budget. an extra store lifts nothing and the objective only reads the fullest one.

ninth day of the summary-vs-set split, third straight time the witness isn't canonical, first time for a new reason. 1552's ambiguity was symmetry - the mirror gave an equally good placement and nothing picked an end. this one is slack. the counts only have to sum to at most n, so a spare store can go to something that isn't the bottleneck, its load drops, the max doesn't move. so what i return is the minimal counts, not the counts.

kept a heap alternate that hands out the n-m spare stores one at a time to whatever currently has the worst even-split load. slower than the bisect on any realistic input. kept it anyway because the last two days both ended with two callers that shared their inner loop, and i'd been counting their agreement as a cross-check when it mostly wasn't one. this one shares no predicate, no ceiling sum, no monotonicity claim. when it agrees, that's evidence.

also got a test expectation wrong from memory and let the assert catch it rather than reasoning about it, which was the right order for once.

## 2026-08-12

wednesday, one problem, mid afternoon. 719, find k-th smallest pair distance. fourth day on the same outer loop, and this time deliberately - yesterday ended on "the shape is a monotone predicate plus a bisect, everything else is whatever answering the predicate takes", and i wanted a predicate that wasn't a feasibility question to see whether that survives.

it survives, but it's incomplete in a way i couldn't have seen from the last three.

the predicate here counts. c(x) = how many pairs sit within x. and because it counts rather than decides, the boundary means something different. the last three all asked for the smallest x that can be done, so "achievable" was the question being asked and the answer being achievable never needed an argument. this asks for an order statistic of a multiset with n(n-1)/2 elements in it, which is a different sort of object entirely, and i'm not building that multiset.

the bridge is that c is the cdf of the distance multiset, so the k-th smallest is where c first reaches k. fine. the part i nearly missed: i bisect over every integer in [0, max-min] and most of those aren't distances between any two elements. so the boundary can land on a value nothing realizes, or at least nothing in the setup forbids it. it can't - if x is minimal with c(x) >= k then c(x-1) < k, so c jumped at x, so some pair is exactly x apart. searching a set bigger than the set of possible answers is repaired by the predicate rather than by the range. first time in four days the answer needed attainment proved and the first time the search space was strictly too big.

second thing, and this is the one that actually cost me time. there are two monotonicity claims in here and i had them fused. the outer one is c non-decreasing in the limit, which is what the bisect leans on. the inner one is left never moving backwards as right advances, which is what makes the counting pass linear instead of quadratic. different variables, neither implies the other, and the inner one would still hold if i only ever called the predicate once. i've been reading "monotone" as a single property of the setup for three days and it's two unrelated facts that happen to share a word.

bounds needed no guard for the third day running. low=0 is legal even when no pair is at distance zero - it just fails and the search steps off it - and high = max-min is the largest element of the multiset so c(high) is everything. same cause as the last two days: read off the structure instead of picked big enough.

tenth day of summary-vs-set and the third distinct reason for the witness not being canonical in four days. 1552 was symmetry, 2064 was slack, this is multiplicity. and this one's stronger than either - the answer is a value and every pair at that separation realizes it identically, so there's no canonical choice even available to define. the other two i could have picked one if i'd wanted to. so the tests check the property.

kept the heap merge, which reads the distances as n-1 already-sorted lists and pops k times. slower on any k near the constraint ceiling and i kept it anyway, for a better reason than yesterday's: the two-pointer sweep is the piece i can't verify by staring at it, and a c that's wrong but still monotone would give me a confident wrong boundary with no symptom. so the sweep also gets checked directly against its definition, every limit from 0 to the spread, which is the assert i'd have skipped a week ago.

walked every k from 1 to n(n-1)/2 on a few small arrays too. the "smallest x with c(x) >= k" phrasing is one off-by-one away from wrong and sampling one k wouldn't find it.

## 2026-08-13

thursday, one problem, late afternoon. 1482, minimum number of days to make m bouquets. fifth day on the same outer loop and the first one where a bound actually needed a guard.

four days in a row i wrote some version of "the bound came off the structure, no guard needed" and by yesterday i'd started treating that as a fact about the technique. it was a fact about the four problems. every one of them was feasible at the top of its range by construction - 2528 always powers every city given enough budget, 1552 always separates two balls, 2064 degenerates to one product per store, 719's multiset obviously contains a k-th element. so "the range is nonempty" and "an answer exists" were the same sentence and i kept getting the second one free without noticing i was taking it.

here they come apart. m bouquets of k adjacent flowers need m*k flowers and waiting never produces a flower that wasn't already going to bloom. so if the garden is too small the predicate is monotone and constant false, and a bisect looking for a boundary that isn't there still returns something.

that's the part worth the day. the failure is silent. drop the guard and low converges to high = max(bloomDay), which is in range and plausible and wrong. every bounds mistake i've made in this run so far announced itself - empty range, index off the end, an assert. this one just hands back a number. so the brute force in the tests is a linear scan with no bounds and no guard at all, which is the only thing that disagrees with the primary exactly where the guard would have been missing.

and the guard isn't a case sitting beside the search, it's what makes the top of the range correct. max(bloomDay) is feasible *because* m*k <= n was already established. i wrote it as an early return and then spent a while convincing myself it wasn't a special case, which it isn't.

the greedy inside the predicate turned out to need almost no argument. within a maximal run of L open flowers you get L//k however you place them, since each bouquet eats k and they can't overlap, so there's nothing to choose and leftmost is optimal by default. that puts this next to 2064 - a formula wearing a loop - rather than next to 2528 and 1552 where the exchange argument was most of the work. three of the five now have a trivial inner greedy. i think the interesting variation in this shape is entirely in the predicate and not at all in the bisect.

yesterday's leftover: 719 searched a range strictly bigger than the set of possible answers and got rescued by the boundary being a jump in the count. here the two sets can be made to coincide, because the bloomed set only changes on days something blooms, so the predicate is piecewise constant and the answer is always an element of bloomDay. wrote the value-space bisect as an alternate. it makes the attainment question disappear instead of answering it, which is nicer, and it's also less general - it needs the pieces known ahead of time. kept the integer version as the primary for that reason.

eleventh day of summary-vs-set and the first repeated reason for the witness not being canonical. symmetry, slack, multiplicity, and now slack again - a run with L % k != 0 has spare flowers and the bouquets slide inside it. the repeat is the thing i noticed. three distinct reasons in a row had me half expecting a fourth, like i was working through a list. slack is just the generic case: when the objective reads a threshold instead of the whole assignment, whatever's left over is free. symmetry and multiplicity were the special ones and i'd been reading them as the pattern.

also had to cut the exhaustive day-by-day checks down to the small gardens after the 1e9 case sat there spinning. obvious in hindsight and a decent illustration of the gap - the search is log in the bloom day and verifying it directly is linear in it.

## 2026-08-15

saturday, one problem, late morning. 774, minimize max distance to gas station. skipped friday, first gap in the run. sixth problem on the same outer loop and the first one where the answer isn't an integer.

i went in expecting that to be a detail about the loop condition. it isn't. it changes what the bisect is allowed to claim.

the setup is the same as always - a station only ever shortens a gap, so needed(x) <= k is upward closed and the bisect finds the boundary. predicate is a ceiling sum with the gaps independent, so no greedy inside it. that's four of six now with a trivial predicate and i'm fairly convinced the variation in this shape is entirely in what the predicate is and not at all in how it's answered.

three things break when the search space is the reals and only the first one is the one people warn about.

termination stops being free. low = mid + 1 on integers strictly shrinks the range every pass, so the loop terminating was a proof. on floats mid can round to low and then low = mid moves nothing and it spins. five days of `while low < high` and i never once thought about progress, because there was nothing to think about. went with a fixed hundred iterations rather than an eps, mostly because the eps form terminates here for a reason that lives in the coordinate constraint rather than in the algorithm, and i'd rather the loop not depend on the input being small.

second, i don't return a point of the search space anymore. i return an endpoint of a bracket and claim the answer is inside it. correctness is a tolerance claim now.

third and this is the actual day. the predicate is allowed to be wrong. every implementation of this i've seen counts with floor(d/x) instead of ceil(d/x)-1, and those differ - floor is one bigger at exact multiples. so the floor version calls infeasible something that's feasible, and the single point it gets wrong is the answer itself. floor-feasible is (ans, inf) where true feasible is [ans, inf).

and it doesn't matter. the bisect never returns a member of the feasible set, it brackets the infimum, and a set and its closure have the same infimum. half-open at one point is invisible to a limit.

that's the thing worth the day. the same substitution on any of the previous five would have been a wrong answer, because there the boundary was a point of the search space and moving one evaluation moved the answer by one. here the boundary is a limit, so the predicate can be wrong on any measure-zero set and nothing moves. what decides it is whether points have weight, and i'd have said "use eps instead of low<high" if asked what changes on the reals. put both counts in the file and asserted the disagreement is exactly the divisors, then asserted the two searches land on the same limit anyway.

kept the greedy heap as the exact alternate. the penalty is max d_i/(c_i+1) and feeding the current argmax is optimal by exchange, so with Fraction keys it gives the answer exactly and not to within anything. i kept it for a specific reason - it shares nothing with the bisect. no predicate, no bounds, no monotonicity claim, no float. the last few days i kept alternates that reused the inner loop and counted their agreement as a cross-check, and mostly it wasn't one.

yesterday's leftover didn't survive contact. 1482's candidate set was the input itself and small enough to bisect over directly, which made attainment disappear instead of proving it. the same set exists here - the answer is d_i/j for some gap and some j <= k+1 - and it has |gaps|*(k+1) elements, two billion at the ceiling. so it's a brute force on toy inputs and nothing more. attained and enumerable are not the same property and i'd been carrying them as one.

twelfth day of summary-vs-set, fourth non-canonical witness running. yesterday i decided slack was the generic case and symmetry and multiplicity were the special ones, so this was the first real test of that and it holds - it's slack, in the most literal version yet. two gaps of 10 and one station to place: penalty is 10 either way, and the station isn't reallocatable so much as inert. same as 2528's unspent budget and 2064's unused stores. three problems now with a resource the objective can't see, always because the objective is a max and a max ignores everything that isn't the argmax.

wrote the inert-station case out as its own assert instead of letting the property check cover it. "the witness isn't canonical" and "the resource does nothing" are different claims and i've now run them together three times.

## 2026-08-17

monday, one problem, before work. 1898, maximum number of removable characters. skipped sunday. seventh problem on the same outer loop and the first one where what i'm bisecting isn't a number.

that sounds like a technicality and it's the whole day. the previous six all searched a quantity that meant something on its own - a budget, a separation, a per-store load, a bloom day, a gap width - and the monotonicity came out of the objective, because more of the resource did more work. here k is an index into removable and that's it. removable[3] isn't bigger than removable[1]; the array isn't sorted and the positions have no order the problem cares about. what k indexes is the set removable[:k], and those nest by construction, and "p is still a subsequence after deleting a set" is antitone in the set. so the predicate is monotone in k because prefixes nest. that's the entire argument. no exchange, no objective, nothing about cost.

i'd been carrying "binary search on the answer" as needing an ordered space of candidate answers with the predicate ordered along it, and the ordered space kept turning out to be the answer's own value because six problems in a row were minimize-a-max. it needs a chain and a predicate constant on each end. a chain of sets is a chain. the notes file i wrote on saturday says the variation lives entirely in the predicate, which i still think is right, but it says it about a family where the search space was the same object every time and i hadn't noticed that was an assumption.

the bottom bound isn't argued, it's promised. k=0 works because the problem says p is a subsequence of s. 1482 is where that stopped being free and the fix there was a computed guard, and the guard hasn't gone away here - it moved into the problem statement. drop the promise and this returns 0 for an input with no answer, in range, silent, the 1482 failure exactly. so the tests assert the promise instead of trusting it, since it's the only thing holding that end up. slightly uncomfortable that the difference between a guard i have to derive and a guard i'm handed is invisible in the code.

direction is the +1 form, since the true half keeps mid. wrote it down without checking against the rule and then checked, which is the first time this run that's been the order.

the predicate is the leftmost-match subsequence greedy, and it has an exchange argument that i almost didn't notice, because the subsequence check is familiar enough to read as a definition rather than as a claim. push each matched position of an arbitrary embedding left to the earliest free slot; later matches only gain room. three of the previous six had a real greedy inside the predicate and three had a formula. this has a greedy and it's the first one where the argument was already in my hands before the problem started, which is probably why i skipped past it.

kept the rank form as the alternate: label each position with the step it gets deleted at, then survival is rank[i] >= k, a threshold on a label. that's the version that says what the search space is - the chain collapses into one function from positions to the time they leave, and the bisect sweeps a threshold across its range. removable is a labelling of s and always was. strictly better than the primary and still second, for 1482's reason.

attainment is free for the first time in the run. three days running it cost something - 719 got it from the jump in the count, 1482 dodged it by bisecting the input values, 774 couldn't have it because the answer set was attained and had two billion elements. here the space is {0..n} and the answer is an index into it. nothing to prove. worth noting that the three hard cases all had a search space bigger than the answer set and this one has them equal, which is what the chain buys.

thirteenth day of summary-vs-set and the first canonical witness. the set still has slack - "ab" in "aab" matches at either a - but the slack has a direction. embeddings are ordered coordinatewise, a least one exists, so leftmost names a member and the greedy already computes it.

that's the distinction i've been missing for twelve days by filing everything under "the witness isn't canonical". the question was never whether the set has more than one element, it nearly always does. it's whether it has a distinguished element. 2528's budget split among tied cities has none because permuting the cities is a symmetry of the whole problem and there's nothing left to break the tie with. positions in a string aren't symmetric. so symmetry isn't one reason among slack and multiplicity - it's the one that actually removes the canonical choice, and slack and multiplicity only looked like they did because those problems happened to be symmetric too. saturday i decided slack was the generic case and the other two were special. today says i had it backwards. first time the test in these files is an equality against the leftmost embedding rather than a property check.

## 2026-08-19

wednesday, one problem, before work. 2616, minimize the maximum difference of pairs. skipped tuesday. eighth on the same outer loop, and the search space is a plain number again after yesterday's chain of sets, which made me think for about ten minutes that this was a filler day.

it isn't, and what makes it not one is the note i left on the 15th. 774 said attained and enumerable are different properties and i'd been carrying them as one. that's been sitting there as a correction with nothing to do, because 774's candidate set had two billion elements and 1482's was the input array, so one made the distinction expensive and the other made it invisible. today is the first case where both hold at once and it takes an argument to get there.

the argument is that an optimal pairing can be taken to use pairs adjacent in sorted order. cross two pairs and re-pair them and the wider gap can only shrink, so nothing that interleaves survives. that collapses the candidate answers from the whole interval [0, max-min] down to the n-1 adjacent gaps. attained because the answer is one of them, enumerable because there are n-1. the candidate-space bisect is then a real second implementation rather than a brute force with a bisect drawn on it, and i ran it against the primary on every case instead of spot-checking, because that agreement *is* the test of the adjacency claim.

second thing, and i nearly wrote the predicate without noticing. there are two exchange arguments here, not one. adjacency decides what the candidate answers are. the leftmost-first scan decides how the predicate gets computed, and it needs its own argument - decline the pair the greedy takes and you're using at most one of those two positions, so swapping costs one and gains one. four of the eight in this run have had a predicate that was a formula with nothing to prove, and after 2064 i'd written down that the exchange argument was never part of the technique. that was 2064 talking. this is the first one with two stacked.

bounds are argued at both ends for once. 1482's low end could be false everywhere and needed a computed guard. 1898's was promised by the statement, which i wrote up as the guard having moved somewhere i couldn't see rather than having gone away. here the top end is a counting fact: at t = max-min the greedy pairs off floor(n/2) and the constraint says p <= n/2. three days of the bound being the interesting part and today it just holds.

fourteenth day of summary-vs-set, and this is the one that breaks yesterday's conclusion, or at least moves it.

yesterday i decided the question isn't whether the witness set has more than one element but whether it has a distinguished one, and that symmetry is what removes the distinguished one. today both answers are true about the same problem. in sorted-position space the witness is canonical for 1898's exact reason - positions have a direction, the scan takes leftmost, done. map it back to the original indices and wherever nums has ties the choice is gone: [1,1,3] with p=1 has one pair in sorted space and two interchangeable elements in the input, and the sort picked between them by input order.

so canonicity isn't a property of the problem. it's a property of the space the witness is named in, and sorting is the step that manufactures it - a total order laid over a multiset that only had a partial one. that's the correction. i'd been checking the problem for symmetry when the thing that decides it is whether the representation already quotiented the symmetry away. 2528's tied cities weren't more symmetric than these duplicates are. they were symmetric in the space i happened to be writing the witness down in, and here i had already left that space before asking the question.

which means the test splits. equality against the leftmost greedy in sorted space, and a property check on the multiset of paired values after reshuffling the input, because the second one is the only version that survives the sort. first time in the run the witness has needed two tests rather than a choice between them.

still uneasy about one thing. if canonicity is representation-relative then the last thirteen days of filing witnesses as canonical or not were all really filing *representations*, and i don't currently have a way to tell which of those calls would flip under a different one. worth going back through them at some point rather than assuming today is the only case.

## 2026-08-20

thursday, one problem, after dinner. 2517, maximum tastiness of candy basket. ninth on the same outer loop, and the first one where i opened the file and already knew the predicate.

this is 1552 with a different noun. baskets on a line became prices on a line, minimum separation became minimum difference, and the greedy is the same left-to-right scan holding one variable. i spent the first ten minutes looking for the part that was new. there isn't one. the algorithm is nine days old and i wrote it on the 10th.

so either today is filler or the repeat is worth something, and what makes it not filler is that it lands exactly on the thing i left open last night.

the note i wrote yesterday said the search space is generically bigger than the answer set and there are three moves available. repair it at the boundary, which is 719. replace it with the answer set, which is 1482 by luck and 2616 by argument. or leave it alone because the answer set is finite and hopeless regardless, which is 774. and i said the property that decides which move you get is the size of the answer set.

here the answer set is every pairwise difference, because the answer is the tight gap inside the chosen basket and the basket is a subsequence, so any two of its members can be the tight one. that's n choose 2. at the constraint ceiling it's 2e8. against thirty predicate calls if you just bisect the range and don't build anything.

and that kills the third branch. i had filed 774's two billion under hopeless as though hopeless were a fact about the number. it isn't. today's set is five times *smaller* than 774's and it's still six hundred times worse than not building it, because the range only ever costs log of the value ceiling. and 2616's n-1 was never a win for being small either - it was a win because n-1 beat log(max-min) on that input. if the prices there had run to 1e18 the same n-1 would have been the same win for a different margin, and if n had been 1e9 it would have stopped being one.

so the deciding property isn't the answer set's size. it's the size against the cost of the search it replaces. two branches and a comparison, and the hopeless branch was never separate - 774 is today with a worse constant. that's the correction, and it's the second night running that something i'd been carrying as a property of the problem turned out to be relative to something outside it. yesterday canonicity was relative to the representation. today enumerable is relative to the alternative. i don't think that's a coincidence so much as what happens when an adjective goes on a checklist and never gets asked compared to what.

kept the candidate search behind a limit that makes it refuse rather than quietly cost n squared. it's a fixture, not a second implementation, and saying so in the signature is better than saying so in a docstring nobody reads before calling it.

bounds, fifth day running, and this time the test threw out my reason rather than my bound. i wrote the top end as "gap max-min admits the two ends and nothing between them, count exactly two" and [7,7,7,7] has span zero, so the top and the bottom are the same point and the count is four. the bound itself is fine - no basket has a gap above max-min because no pair does - and that argument never mentioned the count at all. so i had a correct bound propped up by a sentence that was false on a whole class of inputs. the last four days the missing argument was at least visibly missing. this one was present and wrong. worse, and only caught because i'd started asserting the bounds instead of only writing them down, which i started doing on the 13th for a different reason.

the k=1 hole i caught by reading. the statement asks for a minimum over the pairs of a one-element basket and there aren't any, and if you let the search run it returns max-min for no reason anyone could defend. third boundary hole in nine and the first i found before a test found it.

fifteenth day of summary-vs-set, and for once it confirms rather than moves.

on the 11th i called slack a reason the witness isn't canonical. on the 17th i decided slack was a confound, because every slack case in that stretch was also symmetric. but that was an inference from not having seen the two come apart, which is a weak thing to conclude from, and i've been carrying it since without a case that tests it.

this is the case. [1,2,5,8,13,21] with k=3 answers 8 and both (1,13,21) and (2,13,21) achieve it. two members in the witness set, all prices distinct, nothing interchangeable with anything, no symmetry available anywhere. pure slack. and leftmost still names one of them. so the 17th was right and six days of it looking otherwise were six days of cases that happened to be symmetric too.

ties bring back yesterday's other half unchanged, so the two tests are the same two tests on a witness that's a subsequence now instead of a set of pairs. that part i just transcribed.

what i'd want next is a problem in this shape where the answer set genuinely is the cheaper search - not by a constant, by an exponent. everything in these nine has been a linear or quadratic set against a logarithmic range, which makes the comparison lopsided in the same direction every time and means i've only ever seen one side of the rule i just wrote down.

## 2026-08-21

friday, one problem, late morning before the standup. 2141, maximum running time of n computers. tenth on the same outer loop and the first hard one in a stretch of mediums.

the predicate is a work count and i read it as finished on the first pass. n computers over t minutes want n*t battery-minutes, a battery of size b can hand over min(b, t) of them because it can't power two machines at the same instant, so you need sum(min(b,t)) >= n*t. that's it, no scan, no exchange argument, one line.

and it proves necessity and nothing else. it says the run can't happen without enough work in the pile. it doesn't say the work can be delivered where it's wanted. i didn't notice for most of an hour, and the reason i didn't is that the previous nine all had a greedy inside the predicate, so the object that proved feasibility was already sitting there when the predicate returned true. the scan that counted the balls placed them. the scan that counted the candies chose them. i'd been getting sufficiency for free nine days running and had stopped seeing it as a thing that has to come from somewhere.

sufficiency here is a separate construction. lay every battery end to end on one timeline of length n*t, each contributing min(b,t), then cut into n pieces of length t and read piece j as computer j's schedule. a battery across a cut runs computer j over [a,t] and computer j+1 over [0,x], which clash unless x <= a, and x = min(b,t) - (t-a) <= a exactly because of the cap. the cap at t is what makes the schedule legal and it's the same cap that made the count right. one inequality doing both jobs, which is why the predicate looked complete.

monotonicity doesn't come from a greedy either, first time in ten. both sides of the inequality move with t so there's nothing to read off. what works is g(t) = sum(min(b,t)) - n*t, concave because each min is concave and the subtracted term is linear, g(0) = 0, so the true set is a prefix. i wrote two wrong arguments before that one and both of them were about counting how many batteries sit above the threshold. the right argument doesn't count anything. that's the same mistake as yesterday's bound, which was a correct claim propped up by a sentence about a count, and i made it twice today without recognising it.

bounds, sixth day. genuinely free at both ends this time - zero needs no work, and dropping the min() gives t <= total/n. the assert still found something, smaller than yesterday. i'd written the top-end check as "the top of the range is feasible" because it had been on all nine previous days, and it isn't here: [100,1,1] with n=2 has total//n = 51 and only 53 reachable battery-minutes against the 102 that would need. dropping a term gives you a bound, not a witness. so the top gets tested as a bound and not as a point. nine days of the extreme threshold happening to be achievable and i'd folded that into what a bound is.

the real thing today is a move the framework has no slot for.

sort descending. if the biggest battery holds more than the running average total/n, no schedule can spend all of it, so it's dedicated to one computer, the surplus is unreachable, drop to n-1 computers without it. otherwise nothing is stranded and the answer is floor(total/n). one pass, zero predicate calls, everything expensive in the sort.

the answer set is the n ratios S_k/(n-k) and that scan doesn't search it. it identifies the right member. and everything i've written down over ten days assumed the two branches were "search the range" and "search the answer set", so the whole comparison was between two searches. the question isn't how big the answer set is. it's whether membership is decidable locally, and here it is - battery i is peeled iff it beats the average of what's left, one element and a running sum - and counting candidates would never have shown that.

which leaves the thing i asked for last night, and the answer is no.

i wanted a problem in this shape where the answer set is cheaper by an exponent instead of a constant, because ten problems lopsided the same direction means i've only seen one side of the rule. there isn't one and there can't be. the range is always built out of the input's numbers so its log is capped by the width of a machine integer, thirty to sixty, in every problem of this shape i will ever open. the answer set is always indexed by the input so its size is n, and the constraints let n run to 1e5. one side is capped by the format and the other isn't.

so the lopsidedness isn't a fact about the ten problems i happened to pick. it's forced, and the comparison i spent ten days constructing had its result decided before i opened the first one. third night running that something i was carrying as a property of the problem turned out to be relative to something outside it - the representation on the 19th, the alternative on the 20th, the constraint format tonight - and this one is the worst of the three, because the outside thing isn't part of the mathematics. it's an artefact of the venue. i don't think the rule is wrong. i think i can't test it here, which is a different and more annoying situation.

sixteenth day of summary-vs-set, and it's the first witness in the run the predicate doesn't build, which is the same observation as the necessity/sufficiency split from a different direction.

non-canonical, for symmetry, with nothing else mixed in - permuting the computers is a symmetry of the problem itself rather than a coincidence in the input, so all n! relabelings are equally good and nothing picks one. 2528's tied cities in a purer form. and that lands against yesterday exactly: yesterday was slack with no symmetry available and leftmost still named a member, today is symmetry no rule can break on a problem that has slack too. the two halves separated in both directions on consecutive days, after the 17th ran them together and i spent six days not noticing. that's the test i said last night i'd only seen one side of. i got it a day later on a different thread than the one i asked about, which is the second time this week the useful thing showed up somewhere i wasn't looking.

## 2026-08-22

saturday, one problem, early afternoon. 1802, maximum value at a given index in a bounded array. eleventh on the same outer loop, and the first medium in a while that wasn't just the previous shape with different nouns.

the predicate is O(1). that's the whole day and everything else follows from it. fix the peak v, the cheapest legal array with that peak is the tent max(1, v - |i - index|), and its sum is two arithmetic runs with a clamp. no scan. the previous ten all had a pass over the input inside the predicate and i'd stopped noticing that as a feature of the shape rather than a fact about search.

the canonicity is the thing i didn't expect. seventeenth day of summary-vs-set and the first canonical witness in the run - after two consecutive days of non-canonical for the two different reasons i'd just finished separating. fix v, the legal arrays with v at the index are closed under pointwise min, positivity survives a min and the adjacent-difference bound survives it too, so a nonempty set of positive integer arrays closed under meet has a least element and the tent is it. that's not a tie-break convention. it's a lattice fact about the constraint set, which is a third thing canonical can mean and i only had two.

and meet-closure is why the predicate is closed form. feasibility asks whether some legal array with peak v fits the budget, sum is monotone in the pointwise order, so it asks whether the least one does, and the least one is written down rather than found. one property giving the canonical witness and the O(1) predicate at once. monotonicity too, from the same place - the least array is pointwise non-decreasing in v so its sum is, and it's strictly increasing since the entry at the index is v itself, so no plateau. nine days of reading monotonicity off a greedy and one day of concavity, and this is a third source.

now the part that matters.

the closed form can be solved. three pieces with breakpoints where each side stops clamping, each piece a linear or quadratic inequality in one variable, invert all three and keep the roots that land in their own piece. O(1), zero search.

which answers the 21st, and not in the way i was expecting to be answered. that night i concluded the range-vs-answer-set comparison was decided by the constraint format - log(range) capped by the machine word, |answer set| capped by nothing - so eleven days of building a comparison whose result was fixed before i started, by the venue rather than by the problem. i wrote that i didn't think the rule was wrong, only that i couldn't test it here. true, and also me looking in the wrong place.

both branches of that comparison are searches. a search is what you do with a predicate you can only call. ten days of predicates that were scans - a greedy placing balls, a sweep counting bouquets, a sum over batteries - and every one of them offered exactly one handle, evaluation at a point. bisection is the general method for having no other handle and its cost is the price of that. this predicate is an expression, so it has another handle, and inverting it isn't a cheaper search, it isn't a search.

so the axis was never the size of anything. it's whether the predicate is a black box or an expression. that's a property of the problem, and it's the first thing in four nights that turned out not to be relative to something outside the mathematics - the representation on the 19th, the alternative on the 20th, the constraint format on the 21st, and today something that's actually in the problem.

it also renames what i proposed last night. i said the question was whether membership in the answer set is decidable locally, off the back of 2141's peel. right observation, wrong name on it. the peel worked because the predicate's structure was exposed and local decidability was one symptom. locally decidable and invertible are two ways the same thing shows up, and i'd have kept collecting symptoms if today hadn't been the one where the structure is so exposed there's nothing left to search.

bounds, seventh day. bottom is a promise in the statement again, n <= maxSum makes the all-ones array fit. top is maxSum - n + 1 from keeping only "the other entries cost 1 each", and it's a bound with nothing attaining it - n=3, index=1, maxSum=6 has a ceiling of 4 and an answer of 2, because a peak of 4 in the middle drags both neighbours to 3. third day running for that and it's stopped being the interesting part; the nine days before, where the extreme threshold happened to be achievable every time, are what i should have been suspicious of.

one thing i got wrong and it's a small one with a general shape. i wrote the inversion with a +/- 1 window around each root because isqrt floors and // floors and i assumed two floors compound. they don't. for integer c and real s >= 0, if m = floor((c+s)/2) then 2m - c <= s and 2m - c is an integer so 2m - c <= floor(s), so the inner floor is free. took the window out and asserted the equality against a widened search instead. the assumption cost nothing here because i'd padded it, which is exactly why it would never have got checked - defensive padding hides the question it was protecting against, and i only found out because the padding looked untidy enough to want a reason.
